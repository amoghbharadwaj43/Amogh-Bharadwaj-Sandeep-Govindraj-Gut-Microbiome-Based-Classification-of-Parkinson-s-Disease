# Pinned to the versions used to produce the results in the paper.
# Python 3.11+ recommended. macOS arm64: a bundled libomp is provided in
# vendor/libomp/ and loaded automatically by the XGBoost scripts.

pandas==2.3.3
numpy==2.3.5
scipy==1.16.3
scikit-learn==1.8.0
xgboost==3.1.3
shap==0.50.0
optuna==4.7.0
joblib==1.5.3

networkx==3.6.1
python-louvain==0.16        # provides the `community` module (Louvain)

matplotlib==3.10.8
matplotlib-venn==1.1.1      # provides `matplotlib_venn`
seaborn==0.13.2

statsmodels==0.14.4         # Benjamini-Hochberg FDR (multipletests)
openpyxl==3.1.5             # reading Metcalfe-Roach .xlsx supplementary files
